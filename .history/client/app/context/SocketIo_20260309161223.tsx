import { createContext } from "react"

const SocketContext = createContext<Record<string, string>>({}) // рекорд это обьект 

export default function SocketIo() {
  return (
    <div>
      
    </div>
  )
}
