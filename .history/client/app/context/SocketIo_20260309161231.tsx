import { createContext } from "react"

const SocketContext = createContext<Record<string, string>>({}) // рекорд это обьект с ключами и значениями

export default function SocketIo() {
  return (
    <div>
      
    </div>
  )
}
