import { createContext } from "react"

interface Socket {
    socket: Socket
}

const SocketContext = createContext

export default function SocketIo() {
  return (
    <div>
      
    </div>
  )
}
